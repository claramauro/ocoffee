<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />

        <meta name="description" content="La boutique de café haut de gamme des frères Pepper, une référence pour les amateurs de café de la région de la ville de Hauts-de-Cloques. Un espace chaleureux et accueillant ou ont été sélectionné avec soin les meilleurs grains de café issus de plantations durables et équitables.">
        <title>
            <% if (locals.title) { %>
                <%= title %>
            <% } else { %>
                oCoffee - boutique de café haut de gamme
            <% } %>
                 
        </title>

        <link rel="icon" type="image/x-icon" href="/images/favicon.ico">

        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400..900;1,400..900&display=swap" rel="stylesheet">
        
        <!-- Common CSS Style -->
        <link rel="stylesheet" href="/css/reset.css" />
        <link rel="stylesheet" href="/css/base.css" />
        <link rel="stylesheet" href="/css/componants.css" />
        <link rel="stylesheet" href="/css/common.css" />

        <!-- Style -->
        <link rel="stylesheet" href="/css/<%= page %>.css" />

        <script src="/js/app.js" defer></script>
    </head>
    <body>
        <header class="header">
            <div class="header__container">
                <div class="header__brand">
                    <img class="header__brand__logo" src="/images/logo.svg" alt="Logo de O'Coffee" />
                    <h1 class="header__brand__title">oCoffee</h1>
                </div>
                <button class="btn--mobile-menu" id="btn-menu" aria-label="Ouvrir ou fermer le menu de navigation" aria-expanded="false" aria-controls="mobile-menu">
                    <img src="/images/burger-menu.svg" alt="">
                </button>
                <nav class="header__menu header__menu--desktop">
                    <ul class="nav__list">
                        <li class="nav__list__item">
                            <a class="nav__link" href="/" title="Page d'accueil" <% if (page === 'index') { %> aria-current="true" <% } %> >Accueil</a>
                        </li>
                        <li class="nav__list__item">
                            <a class="nav__link" href="/catalogue" title="Le catalogue de nos produits" <% if (page === 'catalogue') { %> aria-current="true" <% } %> >Catalogue</a>
                        </li>
                        <li class="nav__list__item">
                            <a class="nav__link" href="/store" title="La boutique" <% if (page === 'store') { %> aria-current="true" <% } %> >Boutique</a>
                        </li>
                    </ul>
                </nav>
            </div>
        </header>

        <nav class="header__menu header__menu--mobile hidden" id="mobile-menu" aria-hidden="true">
            <ul class="nav__list">
                <li class="nav__list__item">
                    <a class="nav__link" href="/" title="Page d'accueil" <% if (page === 'index') { %> aria-current="true" <% } %> >Accueil</a>
                </li>
                <li class="nav__list__item">
                    <a class="nav__link" href="/catalogue" title="Le catalogue de nos produits" <% if (page === 'catalogue') { %> aria-current="true" <% } %> >Catalogue</a>
                </li>
                <li class="nav__list__item">
                    <a class="nav__link" href="/store" title="La boutique" <% if (page === 'store') { %> aria-current="true" <% } %>>Boutique</a>
                </li>
            </ul>
        </nav>
